package model;

public class Manager extends Cashier {
    // Constructors
    public Manager() {
        super();
    }

    public Manager(int id, String name, String username, String password) {
        super(id, name, username, password);
    }

    // Manager-specific methods if needed
}
