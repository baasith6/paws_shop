package model;

public class Cashier extends User {
    // Constructors
    public Cashier() {
        super();
    }

    public Cashier(int id, String name, String username, String password) {
        super(id, name, username, password);
    }

    // Cashier-specific methods if needed
}
